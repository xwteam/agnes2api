/**
 * 去掉源码里的注释再扫。**实现在 `scripts/lib/strip-comments.mjs`**，本文件只是转导出。
 *
 * ⚠️⚠️ **这里不再有第二份实现，也不要往这里抄一份回来。** 真源住在 `scripts/` 而不是
 * `tests/helpers/` 是构建物理决定的：`scripts/check-i18n.mjs` 是纯 .mjs、由 `node` 直接跑，
 * 它 import 不了 `.ts`；门禁与测试要共用同一份实现，真源就必须落在两边都 import 得到的那侧。
 * **「用一对正则去抠块注释」的写法为什么不行、本仓为此栽过哪几次，全部写在真源的文件头**，
 * 那里是**那道守卫射程之内**唯一允许出现那条正则字面量的地方。
 * ⚠️ **别把这句写成「全仓唯一」**：`docs/design/` 下的历史计划文档也逐字复述过它
 *（实测那份 P3e 计划正文有 5 行含它），那是正当的史实记录，只是**根本不在射程里**
 *（射程只收 `.ts`/`.js`/`.mjs`，历史文档是 `.md`）——两句话说的是两件事，别再合并成一句。
 *
 * ⚠️ **新的调用点一律 import 这一份（或直接 import 真源），不许再手写一份**
 *——两份不同实现的扫描器给出不同答案时，绿的那一份会赢。
 * 反方向的那张清单由 `tests/unit/source-guards.test.ts` 的
 * 「射程内不许再有第二份用正则抠注释的实现 —— 块注释那一族与行注释那一族都算」那一格钉着，
 * **那一格是断言，不是散文**：它扫的是那几条正则字面量本身，换个函数名躲不掉。
 * ⚠️ **但它认得出的只是「那几种拼法」**：它逐条登记了自己抓不住哪些形态（见那一格旁边的
 * `REGEX_STRIP_MISSES` 表），别把它当成「任何第二份实现都躲不掉」。
 *
 * **三个导出，同一个扫描器**：`stripComments`（JS，删注释）、`blankComments`（JS，注释换空格、
 * 行号列位置不变）、`stripCssComments`（**CSS，只认块注释**）。
 * ⚠️ **抠 CSS 一律用第三个**：CSS 没有 `//` 行注释，拿 JS 语义去抠会把
 * `background: url(//cdn…)` 之后的样式整段吃掉——P3e Task 1 第一版就是这么把两处 CSS
 * 消费者一起弄瞎的（复评实测：违规前面加一个 `url(//…)` 那格就变绿）。
 *
 * **谁在用这一份**：**凡 import 本文件的都算**，一条都不必写在这里。
 * 现场枚举：`grep -rn "^import.*helpers/strip-comments" tests/`。
 * ⚠️ **必须带 `^import`**：裸 grep 会把注释里的提及一起数进来，于是数出来的是
 * 「提到过本文件的地方」，不是「消费者」。
 * ⚠️ **这张消费者清单刻意不写计数**：它是**该长大**的（每多一个调用点收编都是好事），
 * 而本文件曾连着三轮在这里写错数字——写死一个数只会换来机械 bump。
 */
export { stripComments, blankComments, stripCssComments, FAIL_KINDS } from "../../scripts/lib/strip-comments.mjs";
