/**
 * 去掉源码里的注释再扫。**实现在 `scripts/lib/strip-comments.mjs`**，本文件只是转导出。
 *
 * ⚠️⚠️ **这里不再有第二份实现，也不要往这里抄一份回来。** 真源住在 `scripts/` 而不是
 * `tests/helpers/` 是构建物理决定的：`scripts/check-i18n.mjs` 是纯 .mjs、由 `node` 直接跑，
 * 它 import 不了 `.ts`；门禁与测试要共用同一份实现，真源就必须落在两边都 import 得到的那侧。
 * **「用一对正则去抠块注释」的写法为什么不行、本仓为此栽过哪几次，全部写在真源的文件头**，
 * 那里也是全仓唯一允许出现那条正则字面量的地方。
 *
 * ⚠️ **新的调用点一律 import 这一份（或直接 import 真源），不许再手写一份**
 *——两份不同实现的扫描器给出不同答案时，绿的那一份会赢。
 * 反方向的那张清单由 `tests/unit/source-guards.test.ts` 的
 * 「除真源之外，全仓不许再有第二份用正则抠块注释的实现 —— 新抄一份当场红」那一格钉着，
 * **那一格是断言，不是散文**：它扫的是那个正则字面量本身，换个函数名躲不掉。
 *
 * **谁在用这一份**：**凡 import 本文件的都算**，一条都不必写在这里。
 * 现场枚举：`grep -rn "^import.*helpers/strip-comments" tests/`。
 * ⚠️ **必须带 `^import`**：裸 grep 会把注释里的提及一起数进来，于是数出来的是
 * 「提到过本文件的地方」，不是「消费者」。
 * ⚠️ **这张消费者清单刻意不写计数**：它是**该长大**的（每多一个调用点收编都是好事），
 * 而本文件曾连着三轮在这里写错数字——写死一个数只会换来机械 bump。
 */
export { stripComments, blankComments } from "../../scripts/lib/strip-comments.mjs";
