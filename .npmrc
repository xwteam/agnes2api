# **`package.json` 的 `engines.node` 单独不是门禁，这一行才是。**
#
# 实测（同一份 lockfile、同一台机器，两次干净安装）：
# · 不带这一行 ⇒ pnpm 只打一行 `WARN Unsupported engine: wanted {"node":">=…"}`，**EXIT=0**，
#   安装照常完成，`engines` 退化成一句文档。
# · 带这一行   ⇒ `ERR_PNPM_UNSUPPORTED_ENGINE  Unsupported environment`，**EXIT=1**。
#
# 为什么值得把它变成硬失败：本仓的测试用到 Node ≥ 22.13 才有的 `node:module`
# 的 `stripTypeScriptTypes`（`tests/unit/source-guards.test.ts` 里那个常驻裁判）。
# 而**新克隆者在老 Node 上看到的报错与真正的原因毫无关系**——实测 v18.19.1 上
# `pnpm test` 是 vitest 的 `Startup Error`：`rolldown` 里 `import { … styleText } from "node:util"`
# 报 `does not provide an export named 'styleText'`。在安装那一步就用一句
# 「Node 版本不对」把人挡住，比让他去查一个 rolldown 的报错便宜得多。
#
# ⚠️ 这是**仓库级**配置，只作用于在本仓里跑的 pnpm，不动任何人的全局环境。
# ⚠️ 已实测它不会误伤依赖：带着这一行跑 `pnpm install --frozen-lockfile` ⇒ EXIT=0。
# ⚠️ **它管不到哪里，明写**：`Dockerfile` 只 `COPY package.json pnpm-lock.yaml`，
#    这份 `.npmrc` 进不了构建上下文 ⇒ 镜像里那两次 `pnpm install` 不受它约束。
#    那一侧由基础镜像 `node:22-alpine` 自己钉住版本，不必再走 engines 这条路。
engine-strict=true
