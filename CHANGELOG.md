# Changelog

本文件记录项目的所有重要变更。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

> **语言**：本文件只有简体中文一份。繁體中文 / English / 日本語 / 한국어 那几份 README
> 里的版本徽章同样链到这里，点进来看到的就是这一份中文。

## [Unreleased]

### 修复

#### 「测试连接」不再报出它没验过的东西

- **绿灯自己说清它没验凭据。** 这颗按钮只走到「列出可用域名」这一步，而有的邮箱服务
  这一步根本不校验凭据 —— 于是凭据完全填错时它照样报绿，运维据此排除了「凭据有问题」
  这个方向，回头却在建邮箱那一步被拒。现在绿灯那句话里写着它没有验证凭据。
- **一个可用域名都没读到不再算通过。** 它以前渲染成绿灯「可用域名 0 个」，而补池那一步
  没有域名可用会直接失败 —— 两件事在面板上长得一模一样。
- **失败不再一律说成「没有连上」。** 现在按这一次真观测到的状态码分成三档：上游拒绝了
  这次请求（401/403）、上游在限流（429）、以及请求没走通。上游回 401 时我们明明连上了，
  旧文案会把人支去查 DNS 和地址，方向正好反了。失败事件里现在带着结构化的状态码。
- **文档里那句「通了就把通道切过去」跟着改了**（五种语言）：它把「列得出域名」当成了
  「这条通道可用」，正是上面那条误判的文档版。
- 这颗按钮的代价一个字没变：仍然是一次只读 GET，不建邮箱、不注册账号、不消耗任何名额、
  不写存储。分档看的是这一次上游回了什么，不是通道名 —— 上游改了行为，面板的话跟着变。

#### 注册机撞上上游限流之后不再继续尝试

- **注册机撞上上游限流后不再继续打。** 此前撞上限流时会零间隔换下一个域名接着打，
  而上游那两层限流（前置的边缘限流、上游自己的注册限流）的惩罚窗口都比一轮补池长得多，
  **窗口里每打一次请求就把窗口续一次** ⇒ 一轮最多打出 40 次发码请求，且全部注定失败、
  同时把恢复时刻一次次往后推。
  现在撞上任一层限流会**立刻结束这一轮**，并记下一个**退避窗口**；下一轮开跑前先看它，
  **窗口内一次上游请求都不发、一个临时邮箱都不建**。连着撞会指数拉长退避（封顶 4 小时），
  而这里的「连着」数的是**连着几轮一把 key 都没出**：一轮里只要铸出过 key，指数就从头数起
  （那一轮末尾又撞上限流的话回到起步那一档，不接着翻倍）；那一轮一次限流都没撞到的话，
  那把键直接清掉。上一段退避过完之后隔了 4 小时以上都没再撞上，指数同样从头数。
- **注册机现在会记住域名结论。** 此前每次铸 key 都从上游的全部域名里随机取若干个逐个试，
  上一轮刚被拒过的域名下一轮可能再撞一次，而每撞一次都要真建一个临时邮箱、真打一次
  发码请求。现在「这个域名能不能过」会落盘复用，**稳态下一次成功铸号只需要一次发码请求**。
  判定两种 `400`（域名被屏蔽 / 出口被限流）走的是一张**启发式词表**，上游改文案会误判；
  三层护栏压着它（判死要**两轮**——同一轮里被拒几次都只算一次、一轮最多学一个域名、
  排序永不彻底排除任何域名）。**这三层压的只是「好域名被判死」一个方向**：那个方向最坏
  是补池变慢而不是归零，而反方向（真限流被读成域名屏蔽）的后果见下面那两条。
  细节与手动纠正的办法见 REGISTRAR.md。
  同一轮里的几个名额还会**轮着用不同域名**（台账轮内不更新，光按结论排序会让一轮 5 把 key
  全挂在同一个域名下）。

#### 一个好域名被上游拉黑之后，补池不再整轮卡死

- **一个已知能用的域名被上游拉黑之后，补池不会再整轮卡死。** 台账刚落地时还带着一道保险：
  已知能用的域名这次被拒，就改判成「撞了出口限流」而不是「这个域名被屏蔽」。
  它在**上游真把这个域名拉黑**时会一条域名结论都不记 ⇒ 台账里那条「可用」的时间戳永远不
  刷新，而候选排序把最久没用过的可用域名排在最前 ⇒ 它每一轮都排第一，每一轮都是
  「打它一次 → 判成限流 → 停整轮 → 退避翻倍」。实测连着六轮一把 key 都没出，而台账里另外
  几个好域名一次都没被派出去；这个状态最长要等那条「可用」七天后过期才自愈。
  现在分类器判出什么就记什么：那个域名一轮之后降到「待复查」，下一轮换别的候选照常出 key。
  防误判仍然由**两跳规则**接着（一次误判只降一档，一次成功无条件回到「可用」）。
- **同一轮里被上游当面拒过的域名，这一轮接下来的名额会跳过它。** 「一轮最多学一个域名」
  那道钳位在**两个已知能用的域名同时被拉黑**时会把两条结论一起作废，于是台账一个字都不变、
  下一轮的挑法与这一轮完全相同 —— 补池同样会归零到那两条结论过期为止。现在被拒过的域名
  在本轮内排到候选最后（**只在内存里、不落盘、下一轮从头算**），同一轮的下一个名额就能
  落到别的域名上。钳位本身一个字都没改。
- **上游改了限流文案时，补池不再每一轮都打满一批注定失败的发码请求。** 上面那道保险拆掉
  之后留下一维代价，是自己拿探针量出来的：上游一旦改了限流的措辞，真限流会被逐条读成
  「域名被屏蔽」⇒ 本轮不再提前停手，一轮最多打 `MINT_BATCH` 次注定失败的发码请求，
  **而且一个退避窗口都不记**（那两档退避的判据全在上游的字眼上，而这时上游那句限流的话
  **我们的词表一个都没认出来** —— 注意不是「上游没说」，它说了，是我们没认出来）；
  多个域名同一轮被判死又会触发「一轮最多学一个」的钳位 ⇒ 结论整体作废、台账一个字不变
  ⇒ 下一轮逐字节重演。按上游那条已知行为（窗口里每打一次就把窗口续一次），这会把惩罚
  窗口一直续下去。现在多了**第三档退避**：这一轮的域名**成片**被判屏蔽、而这一轮
  一把 key 都没铸出来时，按**这一轮的形状**记一个退避窗口（起步 30 分钟，同样指数、
  同样封顶 4 小时）。「成片」有两条判据，命中任意一条就算：同一轮里有两个以上的域名被判
  屏蔽，**或者上游列出来的域名这一轮一个不落全被判屏蔽**（**只配了一个邮箱域名的部署靠
  的是后一条** —— 它永远凑不满前一条要的那两个）。它**不中止这一轮、也不吞掉任何域名
  结论**，这一轮只要铸出过 key 就一个字都不记 —— 所以上面那把「时间戳冻住」的死锁不会
  回来。面板的退避横幅相应多了一条文案。

#### 第三档退避的代价与那句归属

- **这一档接住之后的请求量，数写进来**（内置值下量出来的）：走到指数封顶之后是**每 8 轮
  打一次、每次 `MINT_BATCH` = 5 个发码请求**，Cron 每 30 分钟一轮 ⇒ **约 30 次/天**。
  两个对照数：接上这一档之前是每轮都打满 ⇒ **约 240 次/天**；而「上游的措辞落进了词表」
  的第二档撞上就整轮停手 ⇒ **约 6 次/天**。**30 不是 6** —— 这一档不中止本轮（本轮该出的
  key 照出），代价就是本轮剩下的名额照打。
- **第三档那条横幅不再说「上游一句限流的话都没说」。** 那句话在这一档要治的头号场景里
  就是假的：上游改了限流文案的时候，上游**明明说了**一句限流的话，只是**我们的词表没认
  出来**。照旧文案读下去，运维会在最该怀疑「上游是不是改了限流文案」的那一刻被明确劝退。
  现在它说的是「上游的回话里没有一句我们认得出的限流的话」，并且**把两种可能都摆出来**
  （上游换了限流措辞 / 上游真的成批换了黑名单），一个方向都不替运维选。
  这与上一版从第二档横幅裁掉的那件事是同一个形状：那次是把「我们的判定」说成「上游的
  事实」，这次是把「我们的词表没命中」说成「上游的事实」。
- **退避横幅不再把「我们自己的判定」说成「上游在限你」。** 那条文案从前写着
  「这个出口地址的注册额度可能已经到顶，多半只能等，或者换一个出口」，而上面那条路径里
  根本没有限流——照它去换出口毫无用处。现在它先说清判据来自上游回话里的字眼、且这是按
  一张词表做的判定而不是上游给的结论，换出口那半句降级成「确认额度真到顶之后再谈」。

#### 第三档横幅指向的那条事件，其中一支根本发不出来

- **第三档横幅不再让运维去翻一条那一支根本发不出来的事件；拿不到全部上游原话这件事
  如实登记。** 上一版横幅与五语言文档逐字写着「分辨两种可能的唯一办法是去看
  `registrar.domain_blocked` 带的上游原话」，而这一档有两支，其中一支上那条事件
  **可证一条都发不出来**：同一轮里好几个域名一起被判屏蔽时，「一轮最多学一条」那道钳位
  会把本轮**全部**「被屏蔽」判定整体作废，台账学不到 `blocked`，那条事件也就没有来源；
  同一轮发出的 `registrar.domain_verdicts_discarded` 只带被作废的条数、本轮产出与退避
  截止时刻，**一个字的上游原话都不带**。现在横幅与五语言文档按支分开写：
  **钳位那一支手里能拿到的上游原话只有 `registrar.known_good_domain_rejected` 那几条，
  而它只覆盖台账里已知能用的那些域名** —— 冷启动、或者上游这一轮新列出来的域名，一句
  原话都没有；「上游列出来的域名全被判屏蔽」那一支才有 `registrar.domain_blocked`。
  **这一条只改说法、不改行为**：事件的字段、退避的算法、钳位本身一个字都没动，
  多出来的那一维代价就写在上面这句里。

### 变更（会改变现有部署的行为，升级前请读）

#### 注册机的三个默认值改了

- `MINT_DELAY_MIN_MS` / `MINT_DELAY_MAX_MS` 的内置取值从 `2000` / `5000` 改成
  `60000` / `90000`，`MAX_DOMAIN_ATTEMPTS` 从 `8` 改成 `1`。
  下限是实测出来的（无间隔连发时第三次就会被上游前置的限流挡下）；上限只是抖动余量。
  **已经在 compose / 环境变量里显式设过这三项的部署不会自动受益**——环境变量的优先级
  最高。请照新的 `.env.example` 更新。
- **空池填满明显变慢**：目标 20 把 key 从「几分钟」变成约 4~5 轮，Cron 每 30 分钟一轮
  ⇒ 约 **2~2.5 小时**。这是把「打光额度还在白打」换成「慢但真的出 key」的直接代价。
- **补池历史新增一种失败归因**：「还在退避窗口里，这一轮一次都没开始」。它与「注册机
  当时是关闭的」是两件事，面板上分得开。
- **注册机面板新增两块**：域名台账（可用 / 判死 / 待复查 / 未探过）与退避状态横幅。
  退避横幅按撞的是哪一层分两种文案，并明说**换一条邮箱通道逃不掉**（限流在出口地址与
  上游之间那条边上，与通道无关）。
- **新增两把存储键**（`registrar:domains` 域名台账、`registrar:backoff` 退避状态），
  各恒为 1 把、都不带 TTL。永久关掉注册机之后清理靠手工删键，与手动补池护栏键同一条。
  写配额账里域名台账给「注册机开着」那两栏各按上界 +48 次/天（稳态下它一次都不写），
  退避键只给「每轮都有失败事件」那一栏 +48（写它的只有撞上限流的轮次，而那些轮次必然
  产出失败事件；「每轮都健康」那一栏是 0）。「立即补池」每次点击的非铸造写侧仍是
  **固定 3 次 put**，另加域名台账与退避键**各最多 1 次条件写 ⇒ 上界 5 次**。

### Changed

#### 注册机通道：从「主备自动降级」改成「二选一」

- **行为变更（能力移除）：注册机的两条邮箱通道从「主备自动降级」改成「二选一」。**
  从前 `REGISTRAR_PRIMARY` 遇到通道级失败（列域名失败、连续建邮箱失败、凭据被吊销、
  收不到验证码）会自动降级到 `REGISTRAR_FALLBACK`；现在只有一条通道，**它失败时不会
  自动改用另一条**。
  - **这把一个自愈的故障换成了一个要人管的故障。** 从前一条通道的 MX 记录失效 /
    凭据被吊销 / 活跃邮箱配额打满，会静静降级到另一条，运维大概率毫无察觉、key 照样
    铸出来；现在同样的故障会让补池**零产出**，池子按 `TARGET_KEYS` 慢慢耗干，几小时
    到几天后以 `pool_empty` 503 的形式炸出来。**升级后请确认你选中的那条通道是能用的**
    ——注册机板块通道卡上那颗「测试连接」就是给这一刻用的，而且它现在对**未选中**的
    通道也能点（见下）。
  - 配置字段 `registrar.primary` 改名为 `registrar.channel`，`registrar.fallback` 删除。
    环境变量新增 `REGISTRAR_CHANNEL`；`REGISTRAR_PRIMARY` **作为兼容别名长期保留**
    （优先级低于新名，生效时面板顶部有一条提示）；`REGISTRAR_FALLBACK` **不再参与选路**，
    但仍会被读取一次，只为在面板上点名告诉你「这条通道被丢掉了」。
    两个旧名都**不会**让升级中的部署停跑——直接删掉它们才会（那会变成「没选通道」，
    而面板会说一个假原因）。`.env.example` 里不再声明这两个旧名，新部署直接用新名字。

#### 通道二选一：存量迁移与接口形状

- **升级中的部署不会停跑，但有一条必须点名的迁移。**
  - **存量存储自动迁移**：`{"registrar":{"primary":"yyds","fallback":"moemail"}}` 里的
    `primary` 会被当作 `channel` 读出来，注册机照常跑；被丢掉的那条通道会在设置页顶部
    显示一条**点名到具体通道**的常驻提示（走 `GET /admin/api/config` 的 `loadNotices`，
    是状态不是一次性事件，不会被 100 格的事件环挤掉，也不多产生一次写）。
    在设置页保存任意一次，两个旧键会被清掉、提示随之消失。
    **如果你的部署实际上一直靠备通道在铸 key**（主通道的凭据早已失效），升级后产出
    会归零，而面板每一格都显示「已配置」。请按那条提示把通道改成它。
  - **未选中的那条通道现在也能测、也能手动补池了**：凭据解析不再只对「链上通道」做，
    于是它的「已配置」是真话、「测试连接」可用、面板「添加 Key ▸ 自动注册」指定它不再
    409。这是「切换前先比一比」需要的一格，也是本次改动里直接变好的一格。
  - 管理接口形状变更：`GET /admin/api/registrar/status` 的 `primary` / `fallback` 两格
    合成 `channel` 一格，`channels[ch].role`（`"primary"|"fallback"|null`）换成
    `channels[ch].selected`（布尔）；`GET /admin/api/overview` 的 `config` 块同改；
    `GET`/`PUT /admin/api/config` 多一格 `loadNotices`。按旧字段名解析的脚本要跟着改。
    补池历史里的 `history.entries[].primaryChannel` **字段名保持不变**——它是已落盘的
    历史格式，改名会让升级前最多 50 条记录被整条丢掉。它的语义就是「这一轮用的那条通道」。
  - 单轮最坏耗时的算式去掉了「× 通道数」这个因子。**顺带修掉一个死局**：默认配置下
    Worker 侧那条「一次尝试都不敢开始、永久零产出」的门槛从 240 秒放宽回 120 秒——
    「配了备通道才会撞上的那个墙钟死局」整个消失。

#### 注册机排障

- **通道请求失败时，错误信息里现在带着它实际请求的地址（已按下面的射程脱敏）。**
  从前事件里只有一行 `YYDS 列域名失败: HTTP 404`，把唯一的线索藏了。最常见的那个坑是
  把 `registrar.yyds.baseUrl` 填成带 `/v1` 的接口地址（网关自己对外的 `AGNES_BASE_URL`
  是带 `/v1` 的，运维顺手就跟着填）⇒ 适配器拼出 `…/v1/v1/domains` ⇒ 404。现在这条
  事件会直接把 `…/v1/v1/domains` 写出来。
  - 脱敏范围：地址里的用户名/密码、查询串、片段一律换成占位标记。
    **凭据写在路径段里（形如 `https://host/t/<某个令牌>/v1`）挡不住**，
    这是已登记的盲区，不要理解成「地址已经全脱敏了」。
  - 为什么要脱敏：`GET /admin/api/events/download` 的设计用途就是被导出、被粘进别处，
    导出的那一刻它就离开了鉴权边界；事件同时进容器 stdout / `wrangler tail`，
    那是另一个信任域。**不是因为事件板块没鉴权——它是鉴权后的。**
  - 两个适配器的验证码轮询**刻意不加**这条日志：单次铸 key 光轮询就打约 40 次请求、
    瞬时错误是常态，在那里留痕会让 100 格的事件环几秒清空一次。代价是「地址对但收信
    接口路径不对」这类故障仍然只表现为 `code_timeout`。
  - 面板：两张通道卡的地址那一格底下多一句事实（填服务根地址、不带接口路径前缀），
    「测试连接」下面那句提示补上「测不通时失败信息里带着它实际请求的地址」。

#### 启动与装配

- **行为变更（未鉴权路径上的契约变更）：Cloudflare Worker 上装配失败的那一档从
  `500` 改成 `503` + `reason: "not_configured"`。按 `500` 报警的监控要跟着改。**
  只有「运维配错」这一档换码（两边都没有 `GATEWAY_TOKEN`、`num()` 的环境变量侧非法值、
  `APIKEY_CACHE_TTL_MS` / `USAGE_FLUSH_INTERVAL_MS` 非法）；其余异常按定义是代码 bug，
  仍然是不透明的 `500`。Node/Docker 侧一行都没改（`process.exit(1)`）。

- **注册机装不起来不再让整个网关死掉。** 从前「开着却缺凭据 / 没选主通道 / 备通道等于
  主通道」会让 `buildApp` 抛错——Docker 上容器反复重启，Worker 上是「部署显示成功、
  每个请求 500、原因只在 `wrangler tail`」（真机实测 81 次探测 60 次 500）。
  现在这一族只让**注册机本次不启动**，转发、`/health`、面板照常。
  - 面板新增第三态「已启用 · 本次没跑起来」，每轮记一条 `error` 级 `registrar.blocked`；
    `GET /admin/api/registrar` 多一格 `blocked`；两条注册机端点回 `409 registrar_blocked`。
  - 设置页那块「装不起来」横幅是**三档**文案，不是两档：整份配置装不起来 / 注册机开着
    但没跑起来 / **注册机关着但配置里仍有几格填得不对**。第三档可达（`delay_min_gt_max`
    那条 blocker 不受 `enabled` 门控），拿第二档的文案糊它会让面板对着一个关着的开关
    说「注册机开着」。
  - 注册机那 16 个环境变量写错值不再让容器起不来（数值类回落默认值 + `config.invalid`）。
    **这是一次能力下降**：运维不能再靠「容器崩了」发现部署笔误。
  - 五份 `DEPLOY.md` 的故障排查条目按运行时拆两段症状，并把 `RESET_CONFIG=1` 从那条的
    解决步骤里删掉（它会连唯一那把口令一起忽略）。

### Added

- **对外 API 密钥**：面板多出第九个板块「API 密钥」（`apikeys`），把「一把网关口令」
  扩成「一把主口令 + N 把可独立签发／命名／到期／停用／吊销的对外密钥」。
  `/admin` 下于是有**九个板块**——概览 `overview`、Key 池 `keys`、注册机 `registrar`、
  事件 `events`、用量 `usage`、模型 `models`、调试台 `playground`、
  API 密钥 `apikeys`、设置 `settings`。
  - **`GATEWAY_TOKEN` 是叠加不是替换**：主口令永远有效，而且判定它**一次存储读都不
    产生**——密钥表被写坏、存储读不出来时，拿主口令调用的客户端一个字节都不受影响。
    这条性质就是逃生口本身，鉴权因此是「先比主口令、没命中才查表」两段。
  - **网关只存密钥的 SHA-256 摘要，明文一个字节都不存**，只在签发那一次的 `201`
    响应里出现过一次。这与上游 key 池刚好相反（那一族必须明文落盘，因为要拿去用）。
  - 新增 `APIKEY_CACHE_TTL_MS`（默认 `300000`）。默认部署（全部客户端用主口令）
    这一族**每天 0 次 KV 读**；用上子密钥之后每个活跃实例每天 `86400 ÷ TTL 秒` 次。
    停用与删除**不是即时的**，上界与代价写在五份 `DEPLOY.md` 的配额账里。
  - 五条端点 `/admin/api/apikeys`（列表 / 签发 / 改 / 删 / 清理失效），
    整表覆写 ⇒ 每次写恒 1 次 put、0 次 delete、0 次 list；乐观并发靠版本号回 `409`。

## [0.1.1] - 2026-08-31

一次整备版。改到的是注释、文案与门禁判据，行为面没有改动：把首个版本之后改动过的
**91 份文件**（`src/` 与 `admin-ui/` 下的 `.ts` 64 + `.js` 14 + `.mjs` 10 + `.css` 3）
逐份抠掉注释再对拍，只有三份仍有差异 —— 两处是字符串里的说明文字（上游事实表里
一条「假设」的描述、面板产物内嵌的注释本身），第三处是版本号常量本身
（`src/version.ts`，发版本来就要改它）。没有一处逻辑改动。
从首个版本升级不需要迁移。

> **下面那些处数是用哪把尺量出来的**：`tests/unit/docs-internal-refs.test.ts` 里
> 登记的四族零宽前后瞻正则（阶段编号 / 内部任务号 / 内部评审发现号 / 内部评审名），
> 对**每一批清理各自的清理前那棵树**逐份现扫，排除控制字符分区名（那两个分区名
> 与发现号同形，是真词）与二进制文件。
> **各条提交标题里的处数是当时用别的写法数出来的，与这里对不上，以这里为准。**
> 换一把尺就会量出另一组数，所以口径写死在这里 —— 别再拿别的写法重数一遍。

### Changed

- **面板资源里的内部研发编号清零** —— 这是这次清理里唯一真正外泄的一块。
  `admin-ui/` 不是只给开发者看的目录：`scripts/build-ui.mjs` 把它**逐字节**烧进
  `src/ui/assets.generated.ts`，`src/ui/serve.ts` 再把那份字节当 `/admin/js/*.js`
  与 `/admin/css/*.css` 的响应体发出去 —— 27 份文件里的 470 处编号，
  **每一个打开面板的访客都会收到**。这 470 处分两轮才清完：第一轮的正则吃不到
  「数字后面还跟一个小写字母」那种形态，漏下的 4 处第二轮补上，
  而那四处同样已经烧进了产物。今天这棵树上判据真扫的结果是 0。
- **源码、测试与门禁脚本里的内部研发编号清掉了绝大部分**：网关源码 501 处（63 份）、
  测试 1854 处（124 份）、门禁脚本 121 处（10 份）、推送前那份逐格表 104 处、
  那一批触及的四份配置 7 处（`.env.example` / `docker-compose.yml` / `wrangler.toml` / `.github/workflows/ci.yml`；`pnpm-lock.yaml` 里那 2 处是 integrity 哈希、同形不算）。**没有清成 0，别把这条读成清零**：网关源码与面板资源今天
  确实是 0，而测试里还剩 142 处、推送前那份逐格表还剩 41 处 —— 剩下的是判据自己
  用来自证的夹具与同形真词，逐条登记在做这件事的那几条提交里。
  **一处都不是删了事** —— 这些编号绝大多数在做指代，
  删掉编号那句话就指不到东西了，所以逐处换成说得清那件事本身的说法，
  行数一行没多一行没少，东亚宽度的分隔线逐条按原宽补齐。
- **出货文档里的内部研发编号清零**：40 份出货文档全集逐份重扫，共 116 处；
  今天那 44 份公开 markdown 上判据真扫的结果是 0，且这条判据每次推送都在跑。
- **提交信息里的内部研发编号清掉了绝大部分**：435 条提交全扫、222 条改写，
  只改信息不动树。**同样没有清成 0**：那一轮的族集比出货文档那条判据窄一族，
  全量提交信息里今天还剩 14 处，逐条登记在做这件事的那条提交里。

### Fixed

- **一条排版上的豁免被静静升级成了泄漏上的豁免**：「出货文档不许留内部研发轨迹
  标识符」这条判据，射程一直借用的是**排版**那根轴的 40 份文档集，而
  `admin-ui/README.md` 不在那 40 份里的唯一依据是一条排版裁定（套统一的章节骨架
  毫无意义）—— 泄漏这根轴一个字都没豁免过它。结构上的后果是那份自述**永远不被
  检查**，它里面 11 处标识符一处都扫不到而门禁全绿。现在泄漏轴单开一份射程
  （44 份：出货文档全集 + 面板那份自述 + `.github` 下全部 `.md`，都从磁盘现算），
  排版轴那 40 份一个字不动，两根轴各钉一格盯住「哪根轴用哪份射程」。
- **那条判据原来的射程只有一份文档**：40 份出货文档里另外 39 份从来没人扫过，
  于是首个版本带着那些编号出了门而门禁全绿。射程改成从磁盘现算的全集之后，
  新增一份文档会自动进射程，不用回来改判据。
- **清理处数原报 91，实测是 116**：差额来自一种会把边界字符一起吃掉的 `grep`
  写法 —— 同一串里连写的两个标识符，它只数得到一个。已加一格夹具钉死这条机制。
- **三格测试一直卡在默认超时的边界上**：它们各自完整跑一遍「注释里的指向必须
  解析得开」那道门禁，实测 2.6–5.8 秒，而测试框架的默认超时恰好是 5 秒 ——
  这三格一直在掷骰子，仓库越大越容易红。只给了显式的时间预算，
  **断言一个字没动**：一条会因为无关原因变红的判据，会训练人忽略它。
- **CHANGELOG 那份能力清单的判据接错了轴**：它判的是「公开仓那份能力清单必须与
  真源逐条对齐」，射程却接在版本号上 —— 于是每发一个补丁版，那十三格都会因为
  「新条目里没抄一遍能力清单」而齐红，而唯一的「捷径」是编造版本条目。
  现在那根轴改接「全部发版条目」，版本号那根轴（当前版本必须有非空条目、
  七处发版日期同一天）原样不动。

## [0.1.0] - 2026-08-31

首个版本。四协议网关、注册机与管理面板一次成型，同一份代码同时跑
Cloudflare Worker 与 Node / Docker 两种运行时。

### Added

#### 网关与转发

- **四协议网关**：`openai`（Chat Completions）、`anthropic`（Messages）、
  `responses`（OpenAI Responses）、`gemini`（generateContent）四条入站协议
  共用同一套上游调度、同一个 Key 池、同一份失败归因。
- **鉴权闸**：网关口令走 `Authorization: Bearer`、`x-api-key`、`x-goog-api-key`、
  查询参数 `?key=` 四条通道，`src/http/middleware/auth.ts` 按这个先后取值，
  正好覆盖各协议官方 SDK 默认发送的那一种，只换基址即可直连；任何通道里传的
  都是**本网关**的口令，不是上游厂商的密钥。
- **流式与媒体**：`openai` / `anthropic` / `responses` 三条协议按请求体里的 `stream`
  字段切流式，`gemini` 换走 `:streamGenerateContent` 那条路径；另有图片与视频
  三条媒体端点 `image.generate` / `video.create` / `video.poll`。这几条的路径、
  上游路径与取正文的位置都从 `src/core/admin/protocol-catalog.ts` 一份真源现取。
- **Key 池与调度**：池索引与取号（Worker 形态落在 KV 上，走 `KvStorage`；
  Node / Docker 形态落在单文件 JSON 上，走 `FileStorage`）、失败归因与冷却；
  上游一个都用不上时回 503，其中「同步档把总预算耗光、一把 key 都没应答」那一种回 504，
  `reason` 逐种可分辨、五语言 API.md 逐份列全。

#### 注册机与管理面板

- **注册机**：`moemail` 与 `yyds` 两条临时邮箱通道，从收码到入池全自动，
  两条通道在文案与取数顺序上严格平级。
- **管理面板**：`/admin` 下的八个板块——概览 `overview`、Key 池 `keys`、
  注册机 `registrar`、事件 `events`、用量 `usage`、模型 `models`、
  调试台 `playground`、设置 `settings`。**零构建**：`admin-ui/` 原样挂在 `/admin/` 下
  就是可调试的面板（`file://` 直接打开不行，理由见 `admin-ui/README.md`），
  `scripts/build-ui.mjs` 只把它逐字节烧进 `src/ui/assets.generated.ts`。
- **管理接口鉴权**：`ADMIN_TOKEN` 没设时 `/admin` 整棵树都不注册；口令只走
  `x-admin-key` 请求头，不落 Cookie、不进 query。

#### 部署、文档与门禁

- **两种部署形态各自的入口**：Worker 形态在六份 README 上各有一颗一键部署按钮，
  按完仍要自己补 `wrangler.toml` 里的 KV 命名空间 id 与 `GATEWAY_TOKEN`，缺一个网关都起不来；
  Node / Docker 形态给 `Dockerfile` 与 `docker-compose.yml`，两处各带一条 healthcheck。
  推 `v*` 标签由 `.github/workflows/docker-publish.yml` 把镜像发到 `ghcr.io`。
- **五语言**：面板与文档（README / ADMIN / API / DEPLOY / REGISTRAR / SPONSORS / USAGE）
  各有 `zh-CN` / `zh-TW` / `en` / `ja` / `ko` 五份。
- **CI 十三道门禁**：跟踪文件不许是二进制、具名放行的 PNG 结构审计、凭据扫描、
  生成面板资源、生成物一致性、面板体积预算、i18n 完整性、KV namespace id 仍是占位符、
  注释里的指向必须解析得开、类型检查、单元 / 契约 / 前端纯函数测试、
  契约测试（workerd 运行时）、构建
  —— 这一串短名逐个对应 `.github/workflows/ci.yml` 里那十三步，顺序也是那边的顺序。

### 已知限制

- **本仓至今零份真上游样本。** `src/core/admin/upstream-facts.ts` 那张上游事实表里，
  每一条的 `status` 都是 `assumed`，`source` 逐条如实写着依据来自我们自己写的假上游；
  契约测试里的上游同样全是桩。所以「示例请求在本仓这份 app 上调得通」
  **不等于**「上游真的接受它」。验后者必须联网打真上游，而真上游要真凭据，
  与本仓「仓库零内置凭据」那条硬约束冲突 —— 这是一条带理由的登记，不是遗漏。
  拿这份协议目录去对接上游之前，请自行核对。
