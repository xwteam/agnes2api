# 管理面板

本文講的是面板上看得見的那一半——每個板塊回答什麼問題、螢幕上那些數字是什麼口徑、哪些動作會寫入儲存。

> [!NOTE]
> 管理面板是**可選的**：沒設 `ADMIN_TOKEN` 時整棵 `/admin` 樹根本不註冊，而閘道照常轉發。
> 本文涵蓋：怎麼進面板，每個板塊逐個過一遍，最後是面板不做什麼與排障。

> [!IMPORTANT]
> **變數本身（預設值、取值範圍、代價）完整的那一份在
> [DEPLOY.md](DEPLOY.md) 的環境變數表裡**，本文一律只給指路，不抄第二份。
> （README 裡另有一張 12 行的常用變數速查表，那是首屏的摘錄，不是第二份真源。）

## 怎麼進面板

### 面板位址

面板掛在閘道自己的網域下，不另開連接埠，也不另起一個服務：

```text
https://<your-gateway>/admin
```

把 `<your-gateway>` 換成你自己的閘道位址。帶尾斜線的 `/admin/` 會自動跳到 `/admin`，
兩種手動輸入的寫法都進得去。

### 前置條件

- **前置條件只有一條**：部署時設好 `ADMIN_TOKEN`。它必須至少 24 位、只由可列印 ASCII
  組成、並且與 `GATEWAY_TOKEN` 不同。這三條各自的理由、邊界，以及不合規時會記下哪條
  事件，都在 [DEPLOY.md](DEPLOY.md) 的「管理面板相關變數」一節。
- **沒設或不合規時，`/admin` 給的是 `404` 而不是 `401`**：整棵樹壓根沒註冊，所以它
  不會順帶告訴外面「這裡有個後台」。面板打不開也**不影響閘道轉發**。

### 口令的存放與撤銷

- **口令原樣存在瀏覽器的 localStorage 裡**，面板會在 12 小時後要求你重新輸入。
- 那個上限只縮短這份 localStorage 值的可用視窗，**不等於撤銷**：撤銷的唯一路徑是換掉
  `ADMIN_TOKEN` 再重新部署 / 重建容器。請把面板放在 TLS 之後，並且只在你信任的機器上
  打開它——完整論證見 [DEPLOY.md](DEPLOY.md) 的「管理口令外洩」那一段。

### 頂欄那一條

頂欄橫貫整個面板，從左到右是：品牌、服務狀態徽章、GitHub 連結、語言、主題、登出；
側欄只剩八項導覽。

- **頂欄上沒有「重啟」按鈕**：Worker 形態沒有可重啟的行程（概覽頁那幾格寫的是
  「無常駐行程」），Node / Docker 形態也沒有自重啟介面。
- **主題切換在登入閘上還有一份**：深色主題在你輸入口令那一刻就已經生效，那一步就該切得動。
- **左上角那枚標記就是 README 開頭那張圖**，瀏覽器分頁上的網站圖示也是它；登入閘上擺的
  同樣是這一枚。兩處都是那張圖縮小之後內聯在面板裡的，並且有一道門禁逐像素比著它們
  ——面板與專案門面不會各長各的。

#### 那顆服務狀態徽章

- **服務狀態徽章有三檔**：運行中 / 已降級 / 狀態未知。資料來源是閘道的 `/health`——那條
  不鑑權、也不觸發任何儲存讀寫的端點。資料目錄可寫時它回 `ok`；不可寫時回 `degraded`，
  同一次應答的狀態碼是 `503`，映像內建的健康檢查認的也是這一條。
- **判不出來的時候它說的是「狀態未知」，不是「運行中」**：網路失敗、應答不是 JSON、
  `status` 是個沒見過的值，三種都歸第三檔。一盞永遠亮著的綠燈與一盞不存在的燈，
  在排障時是同一個資訊量。
- **它說的是「最近一次探測」，不是「此刻」**：面板進入之後探一次，之後點一下徽章重新探。

## 九個板塊速查

| 板塊 | 它回答什麼問題 | 唯讀還是會寫 | 依賴哪個開關 |
|----|--------------|------------|------------|
| 概覽 | 這個部署現在是什麼形態，池子還剩多少可用 key | 唯讀 | – |
| Key 池 | 每一把 key 現在是什麼狀態，為什麼不可用 | 會寫 | – |
| 註冊機 | 自動補池在不在跑，上幾輪補了什麼 | 會寫 | `REGISTRAR_ENABLED` |
| 事件 | 最近發生過哪些維運級別的事 | 唯讀 | – |
| 用量 | 請求量、成功率、延遲隨時間怎麼變 | 唯讀 | `USAGE_STATS_ENABLED`（只管第二層） |
| 模型 | 閘道對外承認哪些模型，各自支援什麼模態與協定 | 唯讀 | – |
| 除錯台 | 用一次真的請求驗一遍某條協定通不通 | 打上游 | – |
| API 密鑰 | 簽發給下游的密鑰各是什麼狀態，哪幾把該吊銷了 | 會寫 | – |
| 設定 | 執行期設定現在是什麼值，哪幾項被環境變數鎖住了 | 會寫 | – |

「會寫」指的是**你在這一頁上按下的動作會寫儲存**；「打上游」指的是真的向上游發一次請求。

> [!IMPORTANT]
> **「打上游」不等於「不寫儲存」**：除錯台發的是一次打到本閘道自己轉發口的真請求，它和
> 任何一次轉發走的是同一條路——這次用掉的那把 key 照樣要記帳，寫入口徑與轉發路徑完全一樣
> （同樣是攢批的），第二層用量開著時還會多記一筆。

## 概覽

概覽回答一個問題：**這個部署現在是什麼形態、池子還剩多少可用 key。**

### 池子與執行期自述

- **池子五個桶**：總數 / 可用 / 冷卻中 / 已剔除 / 已停用。五格恆顯示，一個真實的 `0`
  本來就是真話；**讀不出來時那一格顯示 `—`，絕不偽造 `0`**——這兩件事在螢幕上必須
  長得不一樣。
- **執行期與儲存自述**：執行期名字、版本、伺服端時間、儲存後端、可寫與否，以及
  Node 形態才有的記憶體 / 存活時長 / PID。這些一律取自介面回傳的資料，面板不去嗅探
  自己跑在哪種執行期上；Worker 形態沒有常駐行程，那三格寫的是「無常駐行程」，
  不是 `0`、也不是空白。

### 兩條新鮮度與第一層用量

- **兩條新鮮度**：池快照與設定各有各的「幾秒前讀到的」。它們不是同一條時間線，所以
  分開顯示。池快照那條的遲滯由 `POOL_CACHE_TTL_MS` 決定，還要再疊一層邊緣快取，
  上界與代價在 [DEPLOY.md](DEPLOY.md) 的那一格裡寫著。
- **第一層累計用量**：請求數 / 成功 / 失敗 / 客戶端錯誤 / 成功率。它是整池所有 key 的
  累計聚合，因此標著 `≈`：單把 key 的計數在並行下會少計（KV 沒有 CAS），而且最多晚
  一個 `POOL_TOUCH_INTERVAL_MS` 才寫入。

### 設定摘要

- **設定摘要**：註冊機開關、主備通道、`TARGET_KEYS` 這幾項的目前生效值。被環境變數
  鎖住的欄位帶一個鎖定標記——那幾項在設定頁改了也不會生效，因為環境變數的優先權更高。

## Key 池

### 桶徽章是一次優先權投影

- **桶徽章是一次優先權投影**，不是四個獨立標誌：一把 key 同時滿足多個條件時，按
  已停用 > 已剔除 > 冷卻中 > 可用 取第一個。所以一把「被停用、同時還在冷卻」的 key，
  徽章上寫的是「已停用」——它的冷卻並沒有消失，只是徽章上看不見。

### 面板上的動作

#### 單條動作

- **八個單條動作**：停用、啟用、清冷卻、清失敗計數、取消剔除、改備註、驗活、刪除。

#### 批次動作

- **三個批次動作**：批次停用、批次清冷卻、批次刪除。

### 匯入與狀態重設

- **一次最多匯入 200 把 key**。超過這個數直接 `400`，**不靜默截斷**——截斷意味著你以為
  匯進去了而其實沒有。
- **「重設已存在 key 的狀態」這個勾選框到底重設什麼**：它重設的是**系統判定的失敗態**
  ——失敗計數、冷卻截止時刻、冷卻原因、剔除標記、剔除原因。它**刻意不動**另外四樣：
  停用狀態（那是你自己按下的開關，一次貼上不該把它靜默翻回去）、加入時間（來歷不是
  失敗態）、用量統計（歷史不是失敗態）、備註（你寫給自己看的話）。

### 刪除的前置條件與單把驗活

- **要刪掉一把 key，它必須已停用或已被系統剔除**：兩者滿足其一即可——已被剔除的那些
  不必再手動停用一次。直接刪一把仍在服役的 key 會被拒絕；這條約束在批次路徑上不是整批
  失敗，而是逐項給出原因。
- **單把驗活**會拿這把 key 真的打一次上游。清單裡的「最後使用」與用量計數是攢著寫入的，
  精度最粗到 `POOL_TOUCH_INTERVAL_MS` 這個間隔。

### 改一把 key 的請求與回執

頁面上那幾個單條動作，走的是同一條 `PATCH /admin/api/keys/{id}`：一次請求裡可以同時
帶上「啟用」與「清冷卻」。請求標頭裡的是**面板口令**，不是閘道口令。

```bash
curl -X PATCH http://localhost:8080/admin/api/keys/9f2c \
  -H "x-admin-key: your-admin-token" \
  -H "Content-Type: application/json" \
  -d '{ "disabled": false, "clearCooldown": true }'
```

回執只說這次寫成沒成，不回讀整條記錄——改完的狀態回清單頁看：

```json
{ "ok": true }
```

請求體上每一個欄位的型別與語意在 [API.md](API.md) 裡，本文不抄第二份。

## 註冊機

面板上的註冊機板塊是**索引，不是說明書**：補池的全部規則、通道怎麼選、要自備哪些憑證、
出事怎麼排查，全在 [REGISTRAR.md](REGISTRAR.md) 裡，本文不抄第二份。板塊上看得到的是：

### 兩個分頁

- **這個板塊分成兩頁**：「執行狀態」看現在跑成什麼樣，「設定」改補池的那一套旋鈕與
  兩條通道各自的憑證。**設定頁上沒有第二份**——同一個欄位在整個面板裡只有一個控制項。
- 分頁用左右方向鍵切（Home / End 跳到首尾），整條標籤列在 Tab 序列裡只佔一格。

### 兩條郵箱通道

- **兩條郵箱通道是平級的**，沒有預設值：主通道必須明確指定，備通道可以不配。板塊顯示
  它們各自的角色（主 / 備）與憑證配沒配。

### 補池歷史與通道測試

- **補池歷史**：每一輪什麼時候跑的、走的哪條通道、由定時器還是由人觸發、跑了多久。
- **通道測試只讀取這條通道的可用網域清單**，不建郵箱、不註冊帳號、不消耗任何名額。

### 「立即補池」的四條護欄

- **「立即補池」有四條護欄**，任何一條不滿足都不會啟動：同副本的在途守衛、跨副本的
  儲存短鎖、兩次手動補池之間的最小間隔、每天的手動次數上限。四條各自的錯誤碼、算式
  與殘餘風險在 [REGISTRAR.md](REGISTRAR.md) 的「面板的「立即補池」」一節。
- 註冊機沒打開（`REGISTRAR_ENABLED` 不為 `true`）時板塊照樣看得見，只是狀態顯示為關閉，
  「立即補池」會被後端拒絕。

## 事件

### 這裡記的是哪些事

- 這裡只有**低頻結構化事件**：設定讀取失敗、池索引重建、管理介面登入失敗這類維運診斷，
  加上註冊機的補池事件與面板自己的寫操作。**key 被轉發路徑自動打進冷卻或剔除時目前
  不產出事件**——那一段要回 Key 池板塊看那把 key 自己的狀態。

### 工具列與保留視窗

- **工具列**：按級別篩選、按文字搜尋、暫停自動重新整理、清空、下載。「清空」只清螢幕上
  這一份清單，**不動伺服端已經寫入的事件**；要重看請點「下載」或重新整理頁面。
- **事件按小時分視窗存放，一共留 24 個視窗**——也就是整整一天，更早的會自然過期，
  面板上再也查不到。

### 五條警告條

- 警告條各自在說一件不同的事，**它們是彼此獨立的幾條，可能同時亮起**：

| 警告條 | 它在說什麼 | 你該做什麼 |
|------|----------|----------|
| 丟棄 | 本副本的事件緩衝把最舊的幾條擠掉了，它們沒來得及寫入 | 事件產生得比寫入快；去容器日誌 / Workers Logs 裡找那一段 |
| 預算 | 本副本今天的事件寫入預算已經用完 | 同上：沒寫入的那些仍然在日誌裡 |
| 截斷 | 這一頁沒顯示全部符合條件的事件 | 收窄篩選條件，或者縮短要看的時間範圍 |
| 游標領先 | 擷取位置領先於伺服端時鐘：時鐘回撥，或者副本之間有時鐘偏移 | 面板已經自動從新位置重拉，這一條會自癒 |
| 游標不合契約 | 介面回傳的游標既不是數字也不是空，面板保留了上一個游標——**再往後可能一直看不到新事件** | 查事件儲存是不是被本閘道之外的東西寫過 |

### 一條事件長什麼樣

面板停用一把 key 之後，事件板塊與「下載」拿到的是同一條結構化記錄。事件名是穩定的
機器可讀字串，篩選與分類一律按它走，不解析 `msg`：

```json
{
  "ts": 1735689600000,
  "level": "warn",
  "event": "key.disabled",
  "msg": "面板停用了一把 key（它仍然占着 targetKeys 的名额，不会触发补池）",
  "fields": { "id": "9f2c" }
}
```

## 用量

### 兩層用量

- **用量分兩層，第一層永遠在**。第一層是整池累計的那幾個數（概覽頁頂上那張卡用的就是
  它），不需要任何開關。第二層是按天 / 小時 / 模型 / 協定分解的時間序列，由
  `USAGE_STATS_ENABLED` 控制，**預設是關的**。
- **關著的時候這一頁不畫空圖表**：一張全是 `0` 的圖會被讀成「這段時間沒人用」，而真相
  是「這個部署根本沒在記帳」。這兩件事不許長得一樣。
- **開著、但一條分片都還沒落盤時同樣不許說「沒人用」**。第二層是攢夠一個落盤間隔才寫一次
  的，請求已經發生、數字還在實例記憶體裡——這一頁此時會明說「分不出沒人用和還沒落盤」，
  不替你斷言。這一條與上一條是同一條規矩的兩半：**不許把別的事說成「沒人用」**。

### 時間範圍與保留期

- **四檔時間範圍**：`24h`、`3d`、`7d`、`30d`。
- **最多保留 30 天**，更早的資料已經過期。最長那一檔一次要把整段區間的分片全部讀回來，
  它在 Cloudflare Worker 上是否總能跑完，本倉尚未在真機上量過——失敗時這一頁如實顯示
  為讀取失敗，不會給出一份看起來完整的數字。

### 為什麼這些數標著約等於

- **這裡的數字標著 `≈`，三個來源各不相同**：① 未寫入的尾巴——計數先攢在實例記憶體裡，
  隔一陣才批次寫入，這段視窗有多長面板自己會告訴你；② 短命實例——那一段還沒寫入就被
  回收的話就沒了；③ 同一天裡的分片只有 2 個槽位，同時寫的副本比這更多時，同槽位是
  後寫覆蓋前寫。
- **讀不出來時顯示 `—`，不是 `0`**：「今天真的是 0 次請求」「第二層沒開」「開著但一條分片
  都還沒落盤」「這一次讀失敗了」是四件事，面板不許把它們畫成同一個樣子。

### 按密鑰的用量

- **第二層的分解多了一維：按密鑰。** 點某一天的「下鑽」，除了按小時 / 按模型 / 按協定，
  還有第四張表；「API 密鑰」板塊的每一張卡上也有一行最近一天的用量。
- **主口令的用量歸到保留偽 id `master`**。它**是一個標籤，不是那張表裡的一行**：
  主口令不在密鑰表裡，沒有任何寫路徑能建它或刪它。子密鑰那一側記的是**它的 id**，
  不是名稱、不是摘要、更不是明文——id 不是憑證，它本來就會出現在事件日誌裡。
- **記這一維不給主口令那條鑑權路徑增加任何一次儲存讀或寫**——那條路徑的零儲存存取是
  密鑰表壞掉時的逃生口，不許動。
- **打開這一維不會多花一次寫**。這些數住在原來那個按天的鍵**裡面**，每個實例每天的
  寫次數一次都沒變（那個數與它的演算法在部署文件的配額帳裡）。
- **關著的時候那一行寫「用量：未開啟」，不畫任何一個數字**——畫 `0` 就是把「沒開」
  說成「沒人用」。

### 密鑰刪掉之後，以及為什麼沒有「重置用量」

- **刪掉的密鑰仍然以原始 id 留在歷史裡，面板不會把它標成「已刪除」**。理由不是省事：
  日桶按天存，那個 id 就是簽發時的事實；而要判「已刪除」只能拿它去和目前那張表比對，
  比出來的只是「目前表裡查不到」，而那有兩種成因（真的刪了，或者這一刻那張表讀不出來）
  ——分不開就不下結論。
- **一天之內出現過的不同密鑰數有上界（201 = 密鑰表上限 200 + 主口令那一格）**，
  超出的合併成一格「其它」。日桶是**歷史累計**，而 200 那個上限只管**當下**：
  同一天裡反覆「簽發 → 用一次 → 刪掉」能造出任意多個 id。**溢出丟的是分辨率，不是計數**
  ——總數一條不少。
- **沒有「重置用量」這顆按鈕，而且這是刻意的**。這一層的數按 UTC 日分桶、到期自動過期，
  「重置」在這種形狀上沒有對應語義；真按下去也只是**看起來**清掉了：實例記憶體裡的累加器
  不受影響，最長一個落盤間隔之後它會把整份再寫回來，而多實例部署裡其餘實例照寫不誤
  ——一顆點了會自己回來的按鈕比沒有更糟。要讓某一段歷史消失，等它過保留期。

## 模型

### 這一頁回答什麼

- **這一頁回答的第一件事**：這個閘道對外承認哪些模型、每個模型是什麼類型、在哪幾條協定上
  真的可用、以及該打哪條端點。

### 三種類型與協定可用性

- **三種類型**：對話、圖片、影片。對話模型掛在四條對話協定上；圖片與影片模型的協定那一列
  是空的——它們不屬於任何一條對話協定，走的是各自的媒體端點。
- **協定可用性是一格一格的徽章**，不是一句「支援」或「不支援」：這一列按模型逐條填。

  > [!WARNING]
  > **今天目錄裡沒有一個模型是「一條協定上可用、另一條上不可用」**：那個對話模型四條
  > 協定全可用，三個媒體模型這一列全是空的。可以依協定篩選；篩出來一個都沒有時，這一頁
  > 直接說「這條協定上沒有可用的模型」，不畫一張空表。

### 端點那一列與讀不出來時

- **端點那一列給的是「客戶端該打哪條路徑」**，與 [API.md](API.md) 出自同一份真源，
  本文不抄第二份。
- **目錄讀不出來時這一頁顯示一根破折號**，同螢幕還有一條讀取失敗的橫幅和一顆重試按鈕。

  > [!IMPORTANT]
  > **「讀不出來」與「一個模型都沒有」是兩件事，而今天把這件事講明白的那句文案只活在
  > 破折號的懸停提示裡**（`title=`）：滑鼠停上去看得到，觸控螢幕上出不來。

> [!WARNING]
> **有一處已知的對外分歧，如實登記**：Gemini 那條協定的模型列表介面對**全部**模型
> 一律聲明支援 `generateContent`，**包括那個影片模型**；而影片真正的路徑是「建任務 +
> 輪詢」兩段式。面板這一頁按真實可用性填，兩邊對不上時以這一頁為準。

### 上游模型那張卡

- **這一頁還回答另一件事**：上游帳號此刻回了哪些模型 id。那張卡上有一顆按鈕，按一下會拿
  Key 池裡的一把 key 去真打一次上游，兩次之間要隔一小段時間。
- **它不取代上面那張目錄表，兩張卡並存**：目錄說的是這個閘道支援什麼、拿哪條端點去調；
  上游那份只有一串模型 id，沒有協定歸屬，也沒有端點。
- **兩個方向的差集都畫出來**：上游有而目錄裡沒有的，以及目錄裡有而這次上游沒回的。

  > [!WARNING]
  > **「目錄裡有而這次上游沒回」不等於「上游沒有這個模型」**：上游按帳號發模型，一把權限
  > 較窄的 key 拉回來的清單本來就短。

- **還沒按過那顆按鈕時這張卡是一根破折號**，旁邊跟著一句看得見的說明：「還沒問過上游」與
  「上游一個模型都沒回」是兩件事。
- **上游沒有正常返回時，它的錯誤正文不顯示在面板上**，面板只畫出上游回的狀態碼——那份正文
  正是各家 API 最容易回顯 key 片段的地方。

## 除錯台

### 兩把鑰匙嚴格隔離

- **兩把鑰匙嚴格隔離，這是這一頁最要緊的一件事。** 面板口令走 `x-admin-key`，只打
  `/admin/api` 那棵樹；除錯台用的是**閘道口令**（`GATEWAY_TOKEN`），走各條協定慣用的
  驗證標頭打對外那棵樹。兩把鑰匙分開存在瀏覽器裡，**登出時一起清掉**——共用或
  投影的機器上，不清的那一把會原樣留給下一個人。
- **閘道口令要你手動貼一次，沒有「一鍵填入」**：面板讀不到它的明文（憑證只寫不讀），
  只能把設定頁裡存著的末 4 位與你貼的這一把比一比，然後告訴你「一致」「對不上」
  還是「讀不到，這次比不了」。

### 三種模式

- **三種模式，各自能配的東西不一樣**：

| 模式 | 打的是哪條端點 | 這一檔能配什麼 |
|----|--------------|--------------|
| 對話 | 四條對話協定各自的對外路徑 | 挑協定、挑模型，可以開串流 |
| 圖片 | 圖片生成那一條端點 | 只挑模型；沒有協定選擇器，也沒有串流開關 |
| 影片 | 建任務與查結果兩條端點 | 只挑模型；建完任務自動輪詢結果 |

**協定選擇器與串流開關只在對話檔出現**，不是被藏起來了：媒體那兩條端點在協定目錄裡
不屬於任何一條對話協定，擺一排點不動的按鈕只會讓人以為圖片也能挑協定、也能串流。

### 串流與影片檔

- **串流這一檔不顯示 token 數**：面板在串流下不讀用量，寫一個 `0` 會是假的，所以那一格
  乾脆不給數字。
- **影片是兩段式的**：先建任務拿到任務識別碼，再拿這個識別碼輪詢結果。輪詢**每 5 秒一次，
  上限是 60 次 / 5 分鐘**，先到哪條算哪條——今天這兩條恰好等價（60 次 × 5 秒正好是
  5 分鐘）；頁面被藏起來過的話打點會暫停而牆鐘照走，那時先到的是時長那一條。

> [!WARNING]
> **真實上游生成一條影片要多久，本倉從來沒有量過。** 這兩條上限是拿來封住
> 「一個忘了關的分頁變成一台永動打點機」的，不是從真實生成時長推出來的，本文
> 也不對它們與真實生成時長的關係下任何結論。到上限時面板會明說「輪詢已經到上限，
> 先停下了」並把任務識別碼擺在那裡——任務本身可能還在跑，你可以稍後自己拿這個識別碼再查。

### 每一次發送都是真的打上游

- **建任務的回應裡沒有能用的任務識別碼時，面板不替你猜**：它只把固定的那幾格當識別碼讀，
  都讀不到就不輪詢，並把回應原文原樣擺出來。
- **每一次按「發送」都是真的打上游**：那一次請求與任何一次轉發走同一條路，用掉的那把
  key 照樣記帳（見速查表下面那句）。影片檔一次是 1 次建任務，加上最多 60 次輪詢查詢。
- **螢幕上最多保留 20 輪對話**，更舊的會從這一頁移除，面板會告訴你移除了幾輪；也可以
  按「清空對話」自己清掉。清的只是螢幕上這一份，伺服端什麼都不動。

## API 密鑰

這一頁管的是**你簽發給下游的那一族憑證**。它與「Key 池」方向相反：那一頁裝的是**本閘道
持有的**上游 key（拿去向 Agnes 證明身分），這一頁的每一把都是**別人拿來向本閘道證明
身分**的。兩個詞很像，螢幕上靠這一段與每張卡的卡頭分開。

它是**疊加**在 `GATEWAY_TOKEN` 之上的，不是取代：

- `GATEWAY_TOKEN` 永遠有效，而且判定它**一次儲存讀取都不產生**——密鑰表被寫壞、儲存讀
  不出來時，拿主口令呼叫的客戶端一個位元組都不受影響。這條性質就是本閘道的逃生口，
  所以這一頁刻意不顯示它、也沒有任何按鈕能停用它；要輪換它請到「設定」頁。
- 這一頁簽發出來的每一把可以單獨命名、設到期、隨時停用或吊銷，用來給不同的下游各發
  一把，而不必把主口令分出去。

### 一把密鑰有哪幾格

- **名稱** —— 1–64 個字元，用來認出這把是誰在用；允許重名。
- **遮罩** —— `sk-••••••••` 加末 4 位。**閘道只存密鑰的 SHA-256 摘要，明文一個位元組都不存**，所以遮罩裡沒有前幾位——那幾位我們根本沒有。
- **狀態** —— 啟用中 / 已停用 / 已過期。既停用又過期時顯示「已停用」：那是你自己按下的那個決定。
- **簽發於 / 到期** —— 到期時刻在**簽發那一刻**就算好寫死了。沒有「發出去之後第一次用才開始計時」這種形態。

**上限 200 把**，超了簽發會被拒絕，不會靜默截斷。

### 簽發一把

「簽發密鑰」→ 填名稱 → 選到期（不過期 / 自簽發時刻起 7·30·90 天 / 自訂日期）。

> [!WARNING]
> **明文只在簽發之後那一個對話框裡出現一次。** 關掉它就再也拿不回來了——閘道只存摘要，
> 沒有任何介面能把明文交出來。丟了只能刪掉這把、重新簽發一把新的。
> 這一點與上游 Key 池**剛好相反**（那一族必須明文落盤，因為要拿去用）。

### 停用、吊銷與「多久才生效」

「停用」是可撤銷的（隨時可以再啟用），「刪除」不可撤銷。兩者都不需要先做另一件事——
這裡刪掉的只是一條我們自己簽發的驗證記錄，而吊銷一把已外洩的密鑰要越快越好。

> [!IMPORTANT]
> **停用與刪除不是即時的。** 處理你這次點擊的那個實例立刻生效；別的實例最多還要一個
> `APIKEY_CACHE_TTL_MS`（預設 5 分鐘）再加上 KV 邊緣快取那一段，兩者之和就是上界
> （具體算式見 DEPLOY.md 的配額帳）。面板在你按下之後會把這個具體時長寫在提示裡。要更快就把
> `APIKEY_CACHE_TTL_MS` 調小，代價是讀取配額等量放大（算式見 DEPLOY.md 的配額帳）。

「清理失效」一次刪掉當前**已停用或已過期**的每一把，仍然可用的一把都不動；它刪掉的條數
與兩張統計卡之和逐條相同。

### 這一頁故意不做的三件事

> [!NOTE]
> 這裡原來還有第四條「每把密鑰的用量」，理由是「資料源預設關著，做出來在多數部署上是一整塊恆『未開啟』的介面」。
> **它現在做了**：每張卡上有一行最近一天的用量，而「預設關著」那半沒有被繞過——關著的時候那一行逐字寫「用量：未開啟」，**不畫任何一個數字**。
> 完整說明寫在本頁上面的**用量 → 按密鑰的用量**裡。

- **每把密鑰的消費額度上限** —— 上限要求讀到一個跨實例、近即時的已花額，而這個形態下拿不到：即時用量只在本實例的記憶體裡，落盤還有尾巴。做出來只會是一個帶無界誤差的近似上限。
- **惰性啟用（發出去、第一次用才開始計時）** —— 它要求在鑑權熱路徑上寫儲存，而那條路徑必須零寫。用「自簽發時刻起 N 天」覆蓋同一需求。
- **綁定某幾把上游 key** —— 本閘道的池子按健康度輪詢、是同質的，**沒有「這把對外密鑰只能用這些上游帳號」這個概念**。

## 設定

### 四張卡

設定頁今天有四張卡：

| 卡 | 它管什麼 | 要注意的 |
|----|--------|--------|
| 認證密鑰 | 卡頂是主 API 密鑰的唯讀顯示，下面只有閘道口令一格；管理口令同樣唯讀，面板改不了自己的鑰匙 | 憑證只寫不讀：輸入框留空 = 這一格不改 |
| 上游與冷卻 | 上游位址、逾時，以及冷卻與剔除的那幾個旋鈕 | 其中兩格是建實例時讀一次的，見本節末尾 |
| 整合範例 | 照抄就能跑的呼叫範例 | 位址取自你現在打開面板用的那個網域，口令是佔位符 |
| 危險區 | 兩顆後果不可撤銷的按鈕：重置設定、清空 Key 池 | 兩顆都要二次確認；清空 Key 池那顆還要把目前池大小手打一遍 |

- **主 API 密鑰那一行是唯讀的**：面板只看得到末 4 位、讀不到明文，所以它畫的是遮罩，
  旁邊那顆按鈕複製的是範例佔位符而不是口令本身。明文在部署那一側的 `GATEWAY_TOKEN` 裡，
  也可以用同一張卡裡的輸入框重設一把。

### 四元組與優先權

- **每一格都攤開一個四元組**：儲存裡是什麼、環境變數裡是什麼、現在生效的是哪一個、
  以及是誰把它鎖住的。優先權是**環境變數 > 儲存裡的值 > 內建預設**；被環境變數鎖住的
  格子在這一頁改了不會生效，面板會直接告訴你該去改部署那一側的哪個變數。

### 憑證只寫不讀

- **憑證只寫不讀**：面板拿不到明文，螢幕上只有「已設定 / 未設定」與末 4 位。留空 = 不改；
  真要刪掉得按「清空」，而那一步會先告訴你清完會發生什麼——回落到環境變數裡的值、
  下一次重啟起不來、還是現在不影響任何東西。

### 儲存之後的回執

- **儲存之後面板不只回一句「已儲存」**：它回讀一遍生效值、把真的變了的那幾格標示出來，
  再按這次改的是哪種欄位分岔說傳播。改的是普通欄位時，螢幕上那句話**開頭逐字是
  「本實例已經生效」**，後半句才是「別的副本 / isolate 最長多久才看得到這次改動」——
  它說的是本實例已經生效，不是「哪裡都還沒生效」。那個上界由設定快取與 KV 邊緣快取
  兩層相加得到。

  > [!IMPORTANT]
  > **這兩個數不在環境變數表裡**：它們是 `src/http/config-holder.ts` 裡的
  > 寫死常數，誰都調不了；[DEPLOY.md](DEPLOY.md) 講設定頁儲存回執的那一段**正文**裡寫著取值。

### 兩個例外欄位

> [!IMPORTANT]
> **有兩個例外欄位**：池快照快取與寫消除間隔（`POOL_CACHE_TTL_MS` 與
> `POOL_TOUCH_INTERVAL_MS`）是**建實例時讀一次**的 ⇒ 儲存之後**連本實例都還沒生效**，
> 要重啟容器或者等 isolate 被回收。這次改動**只**碰了這兩格時，上面那句「本實例已經生效
> + 上界」**整條不顯示**，換成的是一句「已經落盤，但本實例也還沒生效」；兩種欄位一起改
> 的話，兩句話同時出現。

### 面板改不了自己的鑰匙

- **面板改不了自己的鑰匙**：`ADMIN_TOKEN` 只從環境變數讀。要輪換它，改部署那一側的
  環境變數再重啟。
- **儲存裡那份設定載入不起來時這一頁變成診斷檢視**：逐條列出缺的是什麼，而且**表單仍然
  可以編輯**——那是唯一的出路。

## 危險區

設定頁的第四張卡。這裡兩顆按鈕的後果都不可撤銷，而且沒有任何撤銷路徑。

| 按鈕 | 動哪些 | 不動哪些 | 二次確認 |
|----|------|--------|--------|
| 重置設定 | 儲存裡那一把設定，一次寫入整把清空，其中包括閘道口令與兩條郵箱通道的憑證 | key 池、每把 key 的用量、事件記錄與補池歷史 | 彈窗先說清重置之後會缺什麼，再讓你確認 |
| 清空 Key 池 | 池裡每一把 key 的記錄，外加那份 id 索引 | 設定、事件記錄與補池歷史 | 除了確認，還要把目前池大小那個數字手打一遍 |

### 重置設定不等於恢復原廠

- **重置設定不等於恢復原廠。** 生效值是「環境變數 > 儲存 > 內建取值」三層壓出來的，
  這顆按鈕只抹掉中間那一層；被環境變數鎖住的欄位一個位元都不動。所以對一部分欄位來說
  它什麼都沒做，而螢幕上會把真的變了的那幾格高亮出來。

### 清空 Key 池會帶走用量歷史

- **清空 Key 池會把用量歷史一起帶走。** 每把 key 的請求數、成功數與最近一次錯誤住在那條
  記錄的值裡面，刪記錄就是刪歷史，沒有第二份可以找回來；key 明文同樣只在儲存裡有這一份。

### 螢幕上做完不等於所有副本都跟上

- **螢幕上做完不等於所有副本都跟上了。** 重置之後，別的副本 / isolate 要等設定快取與邊緣
  快取過去才看得到；清空之後，轉發路徑最長要等一個池快照 TTL 加上邊緣快取才不再選中那些
  key。池快照那條的旋鈕（`POOL_CACHE_TTL_MS`）在 [DEPLOY.md](DEPLOY.md) 的環境變數表裡；

  > [!IMPORTANT]
  > **設定那條不在表裡**——它是 `src/http/config-holder.ts` 裡兩個寫死常數相加，誰都調不了，
  > 取值寫在同一份文件的正文裡。這一頁裡，設定那條在上面的設定卡說明裡也提了一句，
  > 池快照那條在概覽頁講兩條新鮮度的那一條要點裡。

### 配額與憑證

- **兩顆按鈕各自要花多少配額，寫在 [DEPLOY.md](DEPLOY.md) 的配額帳裡**：重置是一次寫入；
  清空是每把 key 一次刪除，外加索引那一次寫入——池子越大，這顆按鈕越貴。
- **兩顆按鈕都不回顯憑證。** 重置的回執裡只有回讀出來的四元組、「配沒配」與那一小截
  末 4 位提示（`hint`，與設定頁憑證欄上顯示的是同一樣東西）——**與這一頁別的地方逐字
  同一條口徑：末 4 位有，明文一個字都沒有。**

## 面板不做什麼

### 不輪換自己的鑰匙

`ADMIN_TOKEN` 只從環境變數讀，面板既改不了它，也沒有「讓已經發出去的工作階段立刻作廢」
這種撤銷路徑。換鑰匙 = 改部署那一側的變數，再重新部署或重建容器。

### 不回顯任何憑證的明文

後端對憑證只回「配沒配」加末 4 位，所以面板做不出「把我的口令填進來」這種按鈕——那需要
在後端開一個明文回顯的口子，而那個口子一開，「憑證只寫不讀」整條就沒了。

### 不代替 wrangler secret 與 .env

這一頁改的是儲存裡那一份設定，而環境變數的優先權更高。兩邊不一致時螢幕上會有鎖定標記，
但把環境變數本身改掉這件事只能在部署那一側做。

### 不替你把遠端媒體拉下來

除錯台裡的結果位址是原樣列出來的，隨時可以複製；**只有 `http` / `https` 開頭的那種才多給
一顆「開啟」連結**——`javascript:` / `blob:` / `file:` 這些即便上游真的返回了也不給，
點開會得到一個由上游內容決定的頂層文件；只有 `data:` 形式的圖片才會內嵌顯示，那一檔
不向任何第三方發請求。

## 排障

### 打不開，而且是 `404`

**原因**：這個部署沒設 `ADMIN_TOKEN`，或者設了但不合規。整棵樹沒註冊，所以外面也看不出
這裡有沒有後台。

**解決**：回部署那一側補上一把合規的 `ADMIN_TOKEN`（三條規則見本文開頭的前置條件），
再重新部署或重建容器。

### 「API 密鑰」頁說密鑰表讀不懂

**原因**：儲存裡的 `apikeys` 這把鍵的結構認不出來（手工編輯過、或者一次寫入被截斷）。
此刻**全部子密鑰都驗不過，而主口令不受影響**——拿 `GATEWAY_TOKEN` 呼叫的客戶端照常。

**解決**：裡面的原始內容**沒有被改動**，面板的寫入操作會被拒絕正是為了不覆蓋它。
先把那把鍵的當前值取出來留檔（Worker：`wrangler kv key get apikeys`；
Docker：`data/store.json` 裡的 `apikeys` 那一項），確認裡面還剩什麼；
然後把它改回 `{"version": <一個數>, "keys": []}` 這個形狀，或者整把刪掉——
刪掉等於把所有已簽發的密鑰一次吊銷，屆時要重新簽發。

### 打不開，而且是 `401`

**原因**：口令本身不對，多半是它已經被輪換過了。

**解決**：把目前那把 `ADMIN_TOKEN` 重新輸入一次。

> [!IMPORTANT]
> **別把這一檔與「面板把你退回登入框」
> 混為一談**：12 小時那條視窗是面板自己判的，到點時它一個請求都不會發出去，伺服端那邊
> 什麼紀錄都不會有。兩種都只要重新輸入一次。

### 打不開，而且是 `503`

**原因**：`ADMIN_TOKEN` 與儲存裡的閘道口令撞成了同一個值。這條規則在每一個管理請求上
複查，事件板塊與日誌裡會有一條 `admin.token_conflict`。

**解決**：把儲存裡的閘道口令換成一個與 `ADMIN_TOKEN` 不同的值。

> [!WARNING]
> **這一步只恢復可用性**：
> 那把管理口令必須按已外洩處置，完整步驟在 [DEPLOY.md](DEPLOY.md) 裡，兩步都做完才算處置完。

### 口令貼進去，登入框當場說這個口令裡有不被接受的字元

**原因**：面板在發請求之前先判字元集，只收可列印 ASCII（`0x20–0x7E`）。碼點超過 `U+00FF`
的漢字、emoji、零寬空格，以及 NUL / LF / CR，瀏覽器根本發不出去；`é` / `£` 這類其實
發得出去，但本閘道基於跨執行時編碼的考量同樣不收。這一檔**一個請求都不會發出去**，
所以伺服端連一條登入失敗都沒有。

**解決**：換一把只由可列印 ASCII 組成的口令再貼一次。

> [!IMPORTANT]
> **部署那一側真配了這樣的口令時
> 你看不到這句話**：那時整棵樹壓根沒註冊，你拿到的是上面那條 `404`。

### 螢幕上是舊值

**原因**：設定與池快照各有各的快取視窗，外面還疊著一層 KV 邊緣快取。池快照那條的
上界與代價在 [DEPLOY.md](DEPLOY.md) 的環境變數表裡；設定那條是寫死常數、**不在那張表裡**，
取值寫在同一份文件的正文裡。本文兩條都不抄第二份。

**解決**：等這兩層快取各自過去，或者重啟容器 / 等 isolate 被回收；概覽頁那兩條
「幾秒前讀到的」就是給這件事看的。

### 報錯是後端原話，不是你的語言

**原因**：面板對**它認得的**錯誤碼顯示本語言文案；碰上它不認得的碼，就把後端那句原話
擺出來，並在旁邊標一句「這句是後端原話，本面板還沒有這條錯誤碼的譯文」。

**解決**：拿後端那句原話去日誌與事件板塊裡對。

> [!IMPORTANT]
> **別讀成「所有錯誤都會用你的語言顯示」**
> ——認不出來的那些不會，而面板寧可把這件事標出來，也不冒充成一句譯文。

## 後續步驟

- 用法與四種介面的 SDK 接入：[USAGE.md](USAGE.md)
- 部署兩種形態與全部環境變數：[DEPLOY.md](DEPLOY.md)
- 註冊機（自動補池）：[REGISTRAR.md](REGISTRAR.md)
- 端點清單與請求 / 回應形狀：[API.md](API.md)
- 專案概況與快速上手：[README.md](../../README.md)
- 回報問題與提問：[GitHub Issues](https://github.com/xwteam/agnes2api/issues)
