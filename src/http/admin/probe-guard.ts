/**
 * 出站探测的护栏。**单把 key 验活与通道连通性测试共用这一份**。
 *
 * ⚠️ **这里刻意不引用「原话」**：上一版写着「原话见 `handlers/registrar.ts` 里
 * `channelTestHandler` 上方那段『如实登记：这条端点没有自己的冷却/预算护栏』」——
 * **而那段话正是被本提交改掉的那一段**（缺口补上了，登记随之作废），
 * 于是这个指向在同一个提交里就腐烂了，**而门禁看不见**：`check-comment-refs` 只验
 * 「文件在不在、名字锚的那串字在不在」，那条引用当时既没带行号也没带名字锚，
 * 它是一句**裸文件名 + 散文复述**。⇒ 改成只说结论、不复述别处的话。
 * 缺口是更早那一轮登记的；今天的处置写在 `handlers/registrar.ts` 的
 * `channelTestHandler` 文档里，**那里是唯一一份，不在这里抄第二遍**。
 *
 * ⚠️ **各写一套就是两套判据**：这两条端点回答的是同一类问题
 *（「按一下就真打一次外网」），护栏也必须是同一条。
 *
 * @refs-ignore（本段引用的是**上一版的原文**，用来记录那次裁定；这里出现的计数是
 * **被引用的对象**，不是本仓在主张一个没人能核的数字。规则 F 会把这份豁免逐处报出来，
 * 由 `tests/unit/check-comment-refs.test.ts` 的「裁定计数的豁免逐处列名 —— 多一处就红」钉成手写清单。）
 * ⚠️ 上一版这里写的是「本仓已经裁过三次『没有单一真源的东西迟早会漂』」——
 * **那个计数删掉了**：仓里复述它的地方有好几处，**从来没有人列出过是哪三次**，
 * 而一个没人能核的计数比一句过期的话更糟（本轮评审裁定）。
 * 这条判据不需要计数来支撑，它自己站得住。
 *
 * 钉住「共用」的是两格，**它们守的不是同一半，缺一半都不够**（两条分工都是变异实测出来的）：
 * · `tests/unit/admin/probe-guard.test.ts` 的
 *   「两条端点的 handler 都从 probe-guard.js 取护栏 —— 各写一套就是两套判据」是**源码级**的，
 *   它拦的是「有人在某个 handler 里另起一份本地实现」。**实测（本任务变异 M11b）：
 *   把 `registrar.ts` 里那次 `tryAcquire` 换成一份行为完全相同的本地实现，
 *   下面那格行为级的用例照样绿**——两份实现今天的行为逐字节相同，行为断言看不见它。
 * · `tests/contract/admin-verify.test.ts` 的
 *   「通道测试与验活共用同一套护栏 —— 两条端点在同一个最小间隔边界上各跑一遍」是
 *   **行为级**的，它拦的是「两处各拿一个自己的间隔常数」。**实测（本任务变异 M11）：
 *   本地实现把常数从 3000 抄成 2000 之后它当场变红**，而源码级那格只查调用点、
 *   查不出一个数。
 *
 * ⚠️ **「同一个实例」这件事今天不可观测，如实登记，别把它读成有护栏。**
 * 两条端点的 kind 命名空间天然不相交（`verify:<id>` vs `channel:<name>`），
 * 所以「共用一个实例」与「各持一个行为相同的实例」在**任何**外部观测上都相等
 *（本仓登记的第 5 种假阳性：覆盖的状态让被测的选择不可观测）。
 * **这不是推的，是量的**：本任务变异 M16 把 `adminRouter` 交给注册机的那个引用换成
 * 第二次 `createProbeGuard()`，`probe-guard` + `admin-verify` + `admin-registrar`
 * **三个文件 68 格全绿、完整逃逸**。
 * ⇒ 它由**结构**保证而不是由断言保证：`adminRouter` 建**一把**并把同一个引用
 * 交给两处，装配上不存在「传错一个」的形态。上面那两格守的是「同一份实现」
 * 与「同一个判据」，不是「同一个对象」。
 *
 * ── 两道闸，缺一不可 ────────────────────────────────────────────────────────
 * · **在途去重**：同一 kind 同时只许有一次在飞。挡的是「连点」。
 * · **最小间隔**：两次之间至少 `PROBE_MIN_INTERVAL_MS`。挡的是「按住不放」。
 *
 * 只有前者时，一次探测在飞期间被挡住的第二次点击会在它一结束就立刻放行，
 * 于是「连点」退化成「跟着上一次的结束节奏连点」，出口 IP 该被打成什么样还是什么样；
 * 只有后者时，两次点击在同一毫秒内到达会**双双**通过（两次都读到同一个 `lastAt`），
 * 而那正是「连点」最常见的形态。
 *
 * ── ⚠️ 它是进程/isolate 内的，不是存储级的 ──────────────────────────────────
 *
 * **这与补池的两把锁不同，别照抄那个形态**（对照表见 `./tend-lock.ts` 文件头）：
 * 补池会消耗邮箱名额与写配额，跨副本必须互斥，所以它在进程内那把之外**另有**
 * 一把存储级短锁；而探测只打上游一次请求，跨副本各自限速已经够了，
 * 为它引入一把存储锁反而会给一条今天零写的端点**新增一次无条件的写**
 *（护栏键是读改写），并且要回去改五语言 DEPLOY.md 的配额账（全局约束 13）。
 *
 * ⇒ **代价明写**：多副本部署下，N 个副本各自允许一次 ⇒ 最小间隔的真实上界是
 * 「每 `PROBE_MIN_INTERVAL_MS` 最多 N 次」，不是 1 次。这与
 * `createTendGate()` 单独存在时挡不住另一个副本是同一句话。
 *
 * ── ⚠️ kind 的空间由调用方限界，本模块自己不限 ────────────────────────────
 *
 * 这个 Map 的键从不主动清理（探测的 kind 数量本来就很小，逐条清理反而要另一个
 * 时间轮）。**它有界的全部理由在调用方**：两条 handler 都把 `tryAcquire` 放在
 * 「这个 id / 这条通道真的存在」之后 ⇒ 键只可能是真实 key 的 id 与两条通道名。
 * 挪到校验之前的话，`POST /admin/api/keys/<随机串>/verify` 就能让任何一个拿到
 * 管理口令的人往这个 Map 里无限灌键。
 * 由 `tests/contract/admin-verify.test.ts` 的
 * 「验一把不存在的 key：连着两次都是 404 而不是第二次 429」钉着——
 * 顺序反了的话第二次会拿到 429，而那正好说明那次不存在的 id 已经在 Map 里落了脚。
 */

/**
 * 两次探测之间的最小间隔。**两条端点共用这一个数**——各写一份的话，
 * 改了一处而没改另一处时不会有任何信号。
 */
export const PROBE_MIN_INTERVAL_MS = 3_000;

export type ProbeAcquire =
  | { ok: true }
  /**
   * **顶层 `reason` 是给面板选文案用的，面板不解析中文 `message`**
   *（与 `handlers/registrar.ts` 里那批 `REASON_*` 同一条口径）。
   * 两种拒绝的处置**完全不同**：`probe_in_flight` 是「上一次还没回来，等它」，
   * `probe_cooldown` 是「刚探过，隔几秒再来」。只给状态码的话两者分不开。
   */
  | { ok: false; reason: "probe_in_flight" | "probe_cooldown"; message: string };

export interface ProbeGuard {
  /**
   * 占住这个 kind。**同步的，里面没有任何 `await`**——与 `TendGate.tryEnter()`
   * 同一条关键性质：写成「先问一句忙不忙、再去占」就把检查与占用之间的窗口造回来了，
   * 而那种形态下第二个并发请求会被放行、真的多打一次外网。
   */
  tryAcquire(kind: string, now: number): ProbeAcquire;
  /**
   * 放开在途标记。**调用方必须放在 `finally` 里**——放在成功支末尾时，一次抛错的
   * 探测会让这个 kind 永久卡在「在飞」，那颗按钮从此再也点不动（最小间隔那道闸
   * 会自愈，这道不会），而「上游连不上」恰恰是运维最想连点重试的那一刻。
   *
   * ⚠️ **这条得有专门的用例守着，别以为顺带就守住了**（本任务变异 M14a 实测：
   * 只把 `verify.ts` 里这一句从 `finally` 挪到成功支，**当时全部 20 格照样绿**）。
   * 判据必须是「失败之后**隔过最小间隔**还能不能再来一次」，只测一次失败看不出来。
   * 两条端点各一格：`tests/contract/admin-verify.test.ts` 的
   * 「上游抛错之后，同一把 key 隔过最小间隔还能再验」与
   * 「上游抛错之后，同一条通道隔过最小间隔还能再测」。
   */
  release(kind: string): void;
}

interface Slot {
  inFlight: boolean;
  /**
   * 上一次**开始**探测的时刻，不是结束的时刻。
   *
   * 取开始时刻，则「最小间隔」度量的是两次**发起**之间的距离，与上游快慢无关；
   * 取结束时刻的话，一次 8 秒超时的探测之后还要再等 3 秒才让人重试——
   * 而上游越慢，运维越想赶紧再试一次。
   */
  lastAt: number;
}

export function createProbeGuard(): ProbeGuard {
  const slots = new Map<string, Slot>();
  return {
    tryAcquire(kind, now) {
      const s = slots.get(kind);
      if (s !== undefined) {
        if (s.inFlight) {
          return {
            ok: false,
            reason: "probe_in_flight",
            message: "上一次探测还没有返回，等它结束再试（这颗按钮每按一次都会真的打一次外网）",
          };
        }
        if (now - s.lastAt < PROBE_MIN_INTERVAL_MS) {
          return {
            ok: false,
            reason: "probe_cooldown",
            message: "两次探测之间至少要隔一小段时间，请稍后再试",
          };
        }
      }
      slots.set(kind, { inFlight: true, lastAt: now });
      return { ok: true };
    },
    release(kind) {
      const s = slots.get(kind);
      // 没占过就放，是调用方的接线错误，但**这里不抛**：一条护栏不该把一个本来
      // 成功的请求变成 500。`inFlight` 保持 false 即可。
      if (s !== undefined) s.inFlight = false;
    },
  };
}
